# Dataset overlay: CodeContests (deepmind/code_contests)
#
# Reward style: stdin/stdout I/O test execution. ground_truth is JSON with
# public_tests, private_tests, generated_tests dicts.
#
# REQUIRES a code-IO reward manager. Test execution is *slow* — expect ~5-30s
# per rollout. Use small batch sizes and few rollouts (n=2) for smoke tests.

data:
  preset: code_contests
  train_batch_size: 32
  max_prompt_length: 4096      # problem statements are long
  max_response_length: 4096
  filter_overlong_prompts: true
  truncation: right
  shuffle: true

reward_model:
  reward_manager: prime
  enable: false

actor_rollout_ref:
  rollout:
    n: 2
